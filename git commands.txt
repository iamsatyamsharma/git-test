Git commands
   github git-test<https://github.com/iamsatyamsharma/git-test.git>
got to that folder     simply type to change directory <cd>
1.git init 
	initialized git repository
2.git status 
	shows current status of repository


	To add file

3.git add . 
	added those file/folder this is presented in current folder
		then - git status -to check status of repository ,it is added or not?
4.git commit -m <name of commit<first commit>>
	save the changes with message
		then - git status
5.git log --oneline
	show us a brief log of all the commit.
5.git log
	show all the commit
6.git checkout <commit no.> <file name>
	checkout the previous commit which is defined.
7.git reset HEAD <filename>
	still show the unstage file
8.git checkput --<filename>
	file restored as next commit.
9.git remote add origin <repository URL>
	-add the remote online repository
10.git push -u origin master
	-push theclocal git repository to the origin to the master branch.
11.git clone <repository URL>
	-clone an online git repository to your computer.